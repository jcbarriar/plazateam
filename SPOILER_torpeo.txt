# Commits
git pull --all

git add <files>
git commit -m "<message>"

git push origin <branch>

==================

# Branches
git checkout <branch>
git checkout -b <branch> #Si no existe esa branch aun

git branch -a

git merge <branch> #Merge de donde estoy ubicado a <branch>

==================

# Configs
git config --global --edit
